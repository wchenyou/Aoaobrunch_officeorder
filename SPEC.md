# 規格與決策紀錄

這份文件記錄「為什麼是現在這樣」。操作步驟看 [README.md](README.md)，改程式碼的注意事項看 [CLAUDE.md](CLAUDE.md)。

---

## 1. 背景

嗷嗷早午餐（台中市西屯區河南路二段 486 號）本來就有企業團購外送服務，也有一份 Google 表單讓公司訂餐。但那份表單的設計是**一家公司送一次**：辦公室裡要有一個人先把大家要吃什麼問完、自己加總成「豬排總匯 3 份、漢堡 2 份、凱薩醬 4 份」這種總數，再填進表單送出。

痛點就在那個人工加總。這套系統把它拆開：每個同事各自點自己的，系統負責彙總，主揪只要按一次送單。

對店家來說收到的東西沒變（一樣是一份完整訂單），對辦公室來說少了一個人工統計的步驟。

---

## 2. 角色與流程

| 角色 | 做什麼 |
|---|---|
| 主揪 | 填一次訂購資訊建團 → 把點餐連結貼到群組 → 截止後按送單 |
| 同事 | 開連結、選餐、填名字、送出；截止前可自行改單或取消 |
| 店家 | 送單時收到一封彙整好的訂單信 |

建團會產生兩個連結：**點餐連結**（`order.html?session=xxx`）公開分享，**管理連結**（`admin.html?session=xxx&admin=權杖`）只有主揪留著，是唯一能看到全部訂單和按送單的入口。

---

## 3. 店家營業規則

來源是店家的外送菜單圖與 Google 表單附註，這些是**店家的規則，不是我們定的**，改之前要先問店家。

- 外送限店面 **3 公里內**
- 單次需滿 **1000 元**才外送
- 最晚需於**預訂日期前一日 15:00 前**下單 ← 系統會擋，見第 6 節
- 外送**只收現金**，不能刷卡
- 不適用店內活動與折價券
- **需經店家致電確認才算訂購成功**（所以聯絡電話與方便接聽時間是必填）
- 外送時段：平日 08:00–15:00、假日 10:00–16:00，誤差半小時內送達
- 外送專線 04-2452-3022

---

## 4. 菜單與價格

取自店家的外送專用菜單圖。這份資料同時寫在 `apps-script/Code.gs` 的 `MENU_SEED`，執行 `setupSheet()` 時填進「菜單」工作表。**之後要改價格改試算表就好，不必改程式碼**（程式碼裡那份只是初始值）。

### 餐點

| 代碼 | 品項 | 價格 | 客製化 |
|---|---|---|---|
| P01 | 嗷飽豬排總匯套餐 | 188 | 醬料（必選） |
| P02 | 美式厚牛漢堡套餐 | 199 | 醬料（必選） |
| P03 | 普羅旺斯烤菇暖沙拉 | 158 | 醬料（必選） |
| P04 | 鮮嫩舒肥雞胸健康餐 | 249 | 醬料（必選）＋雞胸口味（必選） |

醬料選項：凱薩醬／千島醬／塔塔醬／不要醬
雞胸口味：原味海鹽／泰式酸辣

**餐點不含飲料**，飲料是獨立品項。菜單圖註明「圖為內用，外送以紙盒包裝」。

### 飲料

| 代碼 | 品項 | M | L | 備註 |
|---|---|---|---|---|
| D01 | 特調紅茶 | 30 | 35 | |
| D02 | 梔子花綠茶 | 30 | 35 | |
| D03 | 四季春青茶 | 35 | 40 | |
| D04 | 奶香金萱 | 30 | 35 | |
| D05 | 熟果烏龍茶 | 35 | 40 | |
| D06 | 穀物蕎麥茶 | 35 | 40 | 無咖啡因 |
| D07 | 特調紅茶歐蕾 | 55 | 70 | |
| D08 | 梔子花歐蕾 | 55 | 70 | |
| D09 | 四季春歐蕾 | 60 | 75 | |
| D10 | 熟果烏龍歐蕾 | 60 | 75 | |
| D11 | 穀物蕎麥歐蕾 | 60 | 75 | 無咖啡因 |
| D12 | 特調紅奶茶 | 45 | 60 | |
| D13 | 梔子花奶綠 | 45 | 60 | |
| D14 | 四季春奶青 | 50 | 65 | |
| D15 | 熟果烏龍奶茶 | 50 | 65 | |
| D16 | 穀物蕎麥奶茶 | 50 | 65 | 無咖啡因 |
| D17 | 豆漿（有／無糖） | 30 | 35 | 甜度冰塊固定 |
| D18 | 豆漿紅茶 | 35 | 40 | |
| D19 | 美式黑咖啡 | 50 | — | 僅 M |
| D20 | 拿鐵咖啡 | 70 | — | 僅 M，可做熱飲 |
| D21 | 風味拿鐵 | 80 | — | 僅 M，口味：黑糖／焦糖／榛果 |

---

## 5. 資料模型

**2026-09-11 起：Google 試算表換成 Supabase（Postgres）**，欄位對應關係沒變，只是從「一份試算表、四張工作表」變成「一個專案、四張資料表」，原本 `setupSheet()` 一次建好四張表的角色，現在是 `supabase/functions/api/index.ts` 部署時搭配的 migration SQL。以下沿用原本的中文欄位說明，括號內是 Postgres 實際的資料表／欄位名稱。

### 菜單（`menu_items`）
`品項代碼(code)｜品項名稱(name)｜分類(category)｜單價M(price_m)｜單價L(price_l)｜規格1名稱(opt1_name)｜規格1選項(opt1_choices)｜規格1必選(opt1_required)｜規格2名稱(opt2_name)｜規格2選項(opt2_choices)｜規格2必選(opt2_required)｜供應中(available)｜備註(note)｜圖片網址(image_url)`

規格 1／2 是通用欄位，醬料、冰量、甜度、咖啡口味都共用同一套邏輯；選項在 Postgres 裡存成 `text[]` 陣列（試算表年代是半形逗號分隔字串，搬過來時轉成陣列）。`單價L` 留空（`null`）代表只有一種價格。`供應中` 是 `false` 就從點餐頁消失。

### 訂單（`orders`）
`時間戳記(created_at)｜揪團編號(session_id)｜訂單編號(order_code)｜訂購人(name)｜品項代碼(item_code)｜品項名稱(item_name)｜杯型(size)｜規格1選擇(opt1)｜規格2選擇(opt2)｜數量(qty)｜備註(note)｜單價(price)｜小計(subtotal)｜狀態(status)｜通知Email(notify_email)`

一個人點三樣就是三列，共用同一個訂單編號。取消是把狀態改成「已取消」（軟刪除，留紀錄），修改是刪掉舊列、用同一個訂單編號重寫。

### 揪團（`sessions`）
`揪團編號(id)｜主揪(organizer)｜主揪Email(organizer_email)｜建立時間(created_at)｜收單截止時間(deadline)｜取餐方式(fulfillment)｜預訂日期(delivery_date)｜期望送達時間(delivery_time)｜外送地址(address)｜是否需要餐具(need_utensils)｜公司名稱(company)｜統一編號(tax_id)｜聯絡窗口姓名(contact_name)｜聯絡窗口電話(contact_phone)｜方便接聽電話時間(contact_available_time)｜颱風假是否取消(typhoon_cancel)｜備註(note)｜狀態(status)｜管理權杖(token)｜店家出餐進度(vendor_done)｜主揪結案(organizer_closed)`

這些欄位對應店家原表單第二頁的「訂購資訊」。`status` 只有「收單中」和「已送單」兩種值。2026-09-11 原本加了第三種「已完成」給送單後用，但那是主揪、店家共用同一個欄位，誰先標記就把另一邊的畫面也一起改掉，語意搞混，當天稍晚就拆開成 `vendor_done`（店家自己的出餐進度）跟 `organizer_closed`（主揪自己有沒有結案）兩個獨立 boolean 欄位，詳見第 6 節。

### 設定（`settings`）
`key｜value`。店家收單Email、颱風假政策文字、店家電話地址、低消金額、外送範圍公里數、前端網址，加上搬家後新增的**寄件人Email**／**寄信Gmail應用程式密碼**／**寄件人名稱**（Gmail SMTP 寄信設定）跟**店家後台密碼**（`vendor.html` 登入用）。純文字，隨時可在 Supabase 的 Table Editor 改。（`寄信API金鑰` 是搬家初期用過的 Resend 設定，後來改用 Gmail 就沒在用了，留著沒刪。）

---

## 6. 規則與狀態

**收單截止時間有上限。** 建團時若截止時間晚於「預訂日期前一天 15:00」，`createSession_()` 直接擋下來，因為店家收不到。

**截止後全鎖。** 超過截止時間或狀態已是「已送單」，同事就不能新增、修改、取消。前端會把介面收掉，後端 `submitOrderCore` / `updateOrder` / `cancelOrder` 也各自再驗一次，不能只靠前端。

**送單之後，主揪跟店家各自標記自己的「完成」，互不影響。** 主揪在 `admin.html`（用管理權杖）標記的是 `organizer_closed`——代表他自己結算完了（例如跟同事收完錢），標記後這團才會從主揪首頁「你開過的團」消失（那邊只留還需要主揪關注、尚未結案的團）。店家自己在 `vendor.html`（用共用密碼）標記的是 `vendor_done`——代表出餐/準備進度，標記後這團才會從店家後台「待處理訂單」消失。兩個是各自獨立的 boolean 欄位，`completeSession()` 只動 `organizer_closed`，`vendorComplete()` 只動 `vendor_done`，誰標記都不會動到另一邊看到的狀態。

**金額一律後端算。** 前端傳來的價格只用於顯示，後端會重新查「菜單」工作表的單價再乘數量。

**權限只有一層。** 管理權杖是隨機 UUID，拿到管理連結就等於是主揪。訂單的修改連結則是 8 碼隨機碼。這是辦公室訂午餐的信任等級，不適合放機敏資料。

---

## 7. 通知

系統只在兩個時間點寄信，透過 Gmail SMTP（`settings.寄件人Email` + `settings.寄信Gmail應用程式密碼`，直接用一個真的 Gmail 帳號登入 `smtp.gmail.com` 寄信；Apps Script 年代用的是內建的 `MailApp`，搬到 Supabase 後一度試過 Resend，但 Resend 的共用測試網域只能寄給自己，要寄給別人得先買網域驗證，所以改回更直接的 Gmail SMTP）。**2026-09-14 已經設定好應用程式密碼**（寄件人是 sunny30248@gmail.com），實際測試過真的收得到信，寄信功能正式生效。

**建團時** → 寄給主揪，內含點餐連結與管理連結。這是主揪換裝置後找回管理頁的唯一途徑，所以主揪 Email 是必填。

**送單時** → 寄給 `settings` 表裡的店家收單Email（主揪若留了 Email 會一起收到副本）。內容包含訂購資訊（取餐方式、日期時間、地址、餐具、公司統編、聯絡窗口）、依人員的明細、依品項的加總（方便備餐）、總金額，以及未達低消的提醒。

## 7.1 店家後台（`vendor.html`）

送單之後，店家不用等信（尤其信件功能還沒設定好的現在），可以直接開 `vendor.html`、輸入共用密碼，看「待處理訂單」（已送單、`vendor_done` 還是 false 的團，不用選日期）或依日期區間＋完成狀態（全部／尚未完成／已完成，對應 `vendor_done`）查詢。畫面**只顯示依品項彙總的數字（名稱、規格、數量、小計），不顯示跟團者姓名**——店家只需要知道要準備什麼，不需要知道是誰訂的。可以在畫面上把已出餐的團標記完成（只動 `vendor_done`，不影響主揪那邊看到的狀態），也可以把目前查詢區間的資料整包下載成 CSV。手機瀏覽器還能開啟新訂單的推播通知（Web Push，見下一節）。

---

## 8. 決策紀錄

**通知用 Email，不是 LINE 或 Telegram。** 三個都評估過：Telegram 完全免費無限則數但接單人要先加機器人；LINE 最貼近店家習慣但免費方案每月約 200 則推播上限，且 LINE Notify 已於 2025 年停用，要改用 Messaging API 自建官方帳號，設定較繁瑣；Email 設定最簡單，唯一疑慮是「不夠即時」。最後選 Email，因為系統設計成**只在送單時寄一封**，而送單那一刻正好就是店家需要知道的時間點，「不即時」在這個流程裡不構成問題，還順便避免每筆訂單發一封信洗版店家信箱。

**前後端分開放。** 原本是 Apps Script 用 HtmlService 直接出頁面（前後端綁在一起、一次部署搞定），後來依需求改成 GitHub Pages 放靜態前端、Apps Script 只當 JSON API。代價是多了跨網域問題，處理方式見下一條。

**POST 用 `text/plain` 而不是 `application/json`。** Apps Script 不會正確回應瀏覽器的 CORS 預檢（OPTIONS），用 `application/json` 會觸發預檢然後失敗。改用 `text/plain` 這種「簡單請求」的 Content-Type 就能避開，後端照樣把內容當 JSON 解析。

**時間全部手算台北時區。** Apps Script 對沒有時區資訊的時間字串（如 `2026-09-12T15:00`）會依腳本時區解讀，而腳本時區與試算表時區是兩個設定、預設值不一定是台北。所以一律用 epoch 毫秒加減 8 小時自己換算，顯示時明確指定 `Asia/Taipei`。

**主揪找回管理頁做兩層，不做第三層。** 做了 localStorage（同裝置自動回到）和建團寄信（跨裝置）。**刻意沒做**「輸入公司名稱查回管理連結」——公司名稱在辦公室不是秘密，任何同事都能拿到管理權限提前送單。若日後要做，應該做成「輸入公司名稱 → 重新寄信到當初留的信箱」，而不是直接把連結顯示在畫面上。

**管理頁用輪詢不用推播。** Apps Script 沒有 WebSocket。折衷是 20 秒自動更新、切回分頁時更新、加上兩顆手動更新鈕與「最後更新 剛剛」的相對時間。

**共用裝置的逃生門。** 因為「送出後重開連結會自動進修改模式」是靠裝置記憶，辦公室共用電腦時第二個人會看到第一個人的訂單。所以提示會寫出是誰的訂單，並提供「不是我的，我要重點一筆」。這個情境是測試時才發現的。

---

## 9. 尚未跟店家確認的事

這幾件是**依菜單圖與表單推斷**的，如果跟實際不符，改試算表的「菜單」工作表即可，不用改程式碼。

- **飲料的冰量與甜度選項**：菜單圖沒有逐品項列出，是依台灣飲料店慣例補的（正常冰／少冰／微冰／去冰、正常糖／少糖／半糖／微糖／無糖），依據是原表單飲料欄的範例寫著「特調紅茶 大杯 微糖去冰*2」。豆漿標示「甜度冰塊固定」所以只給有糖／無糖。
- **醬料是否四款主餐都適用**：菜單圖上四款下面都印了醬料選擇，所以現在是四款都有。
- **「不要醬」這個選項**：菜單圖只印三種醬，線上表單多了「不要醬」。依使用者指示保留四種。
- **是否提供自取**：店家兩頁表單看到的都是外送流程，但系統有做自取選項。自取是否也受「前一日 15:00」限制目前比照外送辦理。

---

## 10. 測試涵蓋範圍

`tools/test-flow.js`／`test-draft.js`／`test-recover.js` 是 Apps Script 年代寫的，對 `tools/mock-server.js`（本機假後端）跑，現在還能動但已經不是主要防線，跟真正的 Supabase 行為會慢慢兜不起來。

2026-09-11 搬到 Supabase 之後，新功能一律直接打「真的」Supabase 專案（免費、夠快），現在主要靠這幾支：

- **test-supabase-live** — 完整流程對真的 Supabase 跑一次：建團、送出訂單、改單、管理頁統計、送單、標記完成、店家後台查得到、不洩漏跟團者姓名。每次跑完會自己清掉測試資料。
- **test-vendor** — 店家後台：密碼登入（含錯誤密碼、記住密碼）、待處理訂單頁、依日期查詢＋完成狀態篩選、CSV 匯出、標記完成、登出。
- **test-menu-zoom** — 查看菜單按鈕放大完整菜單相片、品項縮圖放大、主揪建團後的「主揪自己也要點餐」按鈕（跳頁不內嵌）、點餐頁「回到管理頁面」只有主揪自己的裝置看得到。
- **test-mygroups-compact** — 首頁「你開過的團」浮窗、角標、孤兒揪團自動清除。
- **test-complete-status** — 主揪標記完成的完整流程（首頁狀態文字、送單後狀態、標記完成後角標消失）。
- **test-split-complete** — 驗證主揪的「標記完成」（`organizer_closed`）跟店家的「標記完成」（`vendor_done`）真的是各自獨立、互不影響：主揪標記完成後店家待處理頁還看得到、店家標記完成後主揪首頁還看得到。

**沒有涵蓋到的**：這些測試都是端對端（透過瀏覽器實際操作），沒有針對 `supabase/functions/api/index.ts` 寫單元測試——例如金額計算、時區解析這些純函式目前只靠端對端流程間接驗證，沒有針對邊界值（例如收單截止時間剛好等於預訂日期前一天 15:00:00）寫過專門的測試案例。CI 也還沒串——這些測試都要手動跑，沒有在 push 或開 PR 時自動執行。

---

## 11. 架構評估：現在的規模適合什麼，之後要擴充要注意什麼

2026-09-14 做過一次完整的端對端測試＋後端程式碼審查（過程中連帶抓到並修掉兩個真的會發生的 bug，見下方），順便留下這份評估，供之後決定要不要、要往哪個方向擴充規模時參考。

**現在的架構，對「一間辦公室、一家店」這個規模是合適的：**
純靜態前端＋一支 Edge Function＋四張表，沒有框架、沒有建置流程，改一行程式碼就能直接部署，符合「使用者要能直接在 GitHub 網頁上編輯檔案」這個明確的設計目標（見 CLAUDE.md〈寫碼慣例〉）。這個規模下，這套架構比起導入框架、真實帳號系統、多租戶資料庫，維護成本低很多、也更容易讓非工程師接手改設定。

**這次審查揪出的兩個真的會發生的 bug（已修掉，不是理論風險）：**
1. 寄信的 MIME 組信邏輯壞掉，收件人看到的是一整段沒解析的原始碼，不是正常信件內容——換成 nodemailer 修掉了。
2. 改訂單是「先刪舊資料、再寫新資料」，中間沒有保護，新資料寫失敗的話訂單會整筆憑空消失——改成「先記舊資料的 id、新資料確定寫成功再刪」修掉了。

**如果之後真的要往「正式、可規模化」的方向走，會依「規模化」實際指的是什麼，分成完全不同量級的工程：**

- **如果是「同時服務更多間辦公室／公司」（多租戶）**：現在的資料模型是單一全域菜單＋單一店家設定，沒有租戶邊界。要做到這個，`menu_items`／`settings` 都要加租戶欄位，店家後台密碼、Gmail 寄信帳號也都要能各自獨立設定，這是資料模型層級的改動，不算小。
- **如果是「同一間辦公室，但訂單量變大、要多人同時操作」**：現在單靠 anon key＋輪詢的架構其實撐得住不小的量（Supabase 的 Postgres＋Edge Function 本身就有一定的擴展能力），比較值得先處理的是 `vendorOrders()` 目前對查詢到的每一團都各自查一次訂單（N+1 查詢），團數一多會變慢，可以合併成一次查詢。
- **如果是「要有真正的帳號系統、更嚴謹的權限控管」**：現在主揪靠隨機權杖、店家靠共用密碼，都是刻意設計成「辦公室訂午餐等級」的信任，見〈已知限制〉。要換成真帳號系統（例如 Supabase Auth）是身分驗證模型層級的改動，會牽動前端每一頁怎麼判斷「這是誰」。
- **如果只是「想要更穩、bug 更少」**：不用大改架構，比較實際的下一步是幫 `supabase/functions/api/index.ts` 裡的核心邏輯（金額計算、時區解析、狀態轉換）補單元測試，並且把現有的端對端測試串進 CI，讓每次改動都自動驗證，而不是像現在這樣要手動記得跑。

這幾個方向彼此不衝突，可以視實際需求各自挑著做，不必為了「規模化」三個字就整套換掉現在這套簡單、好維護的架構。
